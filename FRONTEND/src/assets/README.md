Reserved for static assets (images, icons, fonts) as the app grows.

Currently no local asset files are used — icons come from `lucide-react`
and all colors/styling are inline Tailwind classes, so this folder is
empty for now.
